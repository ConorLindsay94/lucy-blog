import { mq } from "../global";

export const styles = {
  header: {
    textAlign: "center",
    [mq[2]]: {
      textAlign: "left",
    }
  },
  description: {
    textAlign: "center",
    [mq[2]]: {
      textAlign: "left",
    }
  }
}